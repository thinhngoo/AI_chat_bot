import 'package:flutter/material.dart';

/// Application color constants
class AppColors {
  // Private constructor to prevent instantiation
  AppColors._();

  // Main colors
  static const Color background = Color(0xFF0A0A0A);
  static const Color foreground = Color(0xFFFFFFFF);

  // Button colors
  static const Color button = Color(0xFF1B1C20);
  static const Color buttonForeground = Color(0xFFFFFFFF);

  // Input field colors
  static const Color input = Color(0xFF131416);
  static const Color inputForeground = Color(0xFFD9D9D9);

  // Muted text color
  static const Color muted = Color(0xFF7F868C);

  // Border colors
  static const Color border = Color(0xFF2C2C2C);
  static const Color ring = Color(0xFFD4C9C9);

  // Additional colors
  static const Color error = Color(0xFFE57373);
  static const Color success = Color(0xFF81C784);
}
