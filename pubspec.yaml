name: chatbot_app
description: A simple chatbot app using DeepSeek API
version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_auth: ^4.16.0 # Upgrade to a compatible version
  http: ^0.13.3
  flutter_dotenv: ^5.0.2 # Đọc file .env
  logger: ^2.0.2
  firebase_core: ^2.24.2
  google_sign_in: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true